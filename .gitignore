.claude/
.DS_Store

# Node / Electron build output (see app/.gitignore for the rest)
node_modules/
app/dist/
app/dist-electron/
app/release/
app/.angular/

# OAuth credentials — real secrets, never commit
app/electron/credentials.json
